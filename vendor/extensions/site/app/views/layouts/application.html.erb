<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
  <title><%= title + ' | ' + t('title_home') %></title>
  <meta http-equiv="Content-Type" content="text/html;charset=utf-8" />
 
  <%= stylesheet_link_tag 'compiled/screen' %>
  <link type="text/css" rel="stylesheet" media="screen" href="/stylesheets/site.css">
 
  <%= javascript_include_tag 'jquery-1.3.2.min', 'jquery.validate.pack.js', 'jquery.tools.min.js', :cache => 'jquery-and-plugins' %>
  

  
  <%= meta_data_tags %> 
  <%= yield :head %>
   <meta name="google-site-verification" content="N8YjFCR2Al_4vccPhV_7tG_McsETq5sg5k1EfTr92wo" />
</head>
<%
if @body_class.nil?
  if @content_for_sidebar
    @body_class = 'two-col'
  else
    @body_class = 'one-col'
  end
end
 %>
<body class="<%= @body_class %>"<%= %( id="#{@body_id}") if @body_id %>>
  <div id="header" class="container">
    <div id="nav">                                                           
    <ul id="nav-bar">
      <div id="login-bar">
        <%= render 'shared/login_bar' %>
      </div>
      <% if store_menu? %>
        <%= render 'shared/store_menu' %>
      <% end %>
    </ul>
    
	<a title="Inicio" href="http://www.qpomo.com">
	   QPomo.com 
	</a>
	</div>
	
	<div id="header_title">
	    <div class="left">
	   <img src="/images/qpomo.gif" alt="pomos para puertas" width="80px" id="logo" />
	   </div>
	   <div class="right">
	   <h1><%= t('header_home') %></h1>
	   </div>
	</div>
  </div>
  
  <div id="two-columns">
	  <div id="wrapper" class="container"> 
	    <% if @content_for_sidebar %>
	    <div id="sidebar">
	      <%= yield :sidebar %>
	    </div>
	    <% end %>
	    
	    <div id="content">
	      <%= breadcrumbs(@taxon) if @taxon %>
	      <% if flash[:notice] %>
	      <div class="flash notice"><%= flash[:notice] %></div>
	      <% end %>
	      <% if flash[:error] %>
	      <div class="flash errors"><%= flash[:error] %></div>
	      <% end %>
	      <%= yield %>
	    </div>
	    <%= render :partial => 'shared/footer'%>   
	     		
	  </div>
	  <% if @products %>
	  <%= render :partial => "shared/scrollable_gallery.html.erb", :locals => {:products => @products, :taxon => @taxon } %>
	   <% end %>
   </div>
</body>
</html>
